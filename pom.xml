<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/>
    </parent>

    <groupId>com.memorylink</groupId>
    <artifactId>memorylink</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <name>memorylink</name>
    <description>MemoryLink Learning System</description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Thymeleaf -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- JPA / Hibernate -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Password Encoder -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <!-- Development Tools -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>

        <!--
            IMPORTANT:
            Java files are located in the ROOT directory.
        -->
        <sourceDirectory>${project.basedir}</sourceDirectory>

        <resources>

            <!-- application.properties -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>application.properties</include>
                </includes>
            </resource>

            <!--
                IMPORTANT:
                Root HTML files are moved into
                classpath:/templates/
                so Thymeleaf can find them.
            -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>*.html</include>
                </includes>
                <targetPath>templates</targetPath>
            </resource>

            <!-- Root CSS files -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>*.css</include>
                </includes>
                <targetPath>static</targetPath>
            </resource>

            <!-- Root JavaScript files -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>*.js</include>
                </includes>
                <targetPath>static</targetPath>
            </resource>

            <!-- SQL files -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>*.sql</include>
                </includes>
            </resource>

            <!-- Images -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>images/**</include>
                </includes>
                <targetPath>static/images</targetPath>
            </resource>

            <!-- CSS folder, if present -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>css/**</include>
                </includes>
                <targetPath>static/css</targetPath>
            </resource>

            <!-- JS folder, if present -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>js/**</include>
                </includes>
                <targetPath>static/js</targetPath>
            </resource>

            <!-- Assets folder, if present -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>assets/**</include>
                </includes>
                <targetPath>static/assets</targetPath>
            </resource>

        </resources>

        <plugins>

            <!-- Maven Compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>

            <!-- Spring Boot -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.memorylink.MemoryLinkApplication</mainClass>
                </configuration>
            </plugin>

        </plugins>

    </build>

</project>
